<snippet>
  <content><![CDATA[
func ($this) String() string {
  ${0:$indented_selection}
}
]]></content>
  <tabTrigger>ds</tabTrigger>
  <eval><![CDATA[
  {
    'this': \
      str(
        (
          __import__('Expression').
            expression.
            find_match(
              view,
              view.sel()[0].a,
              'func\s*\((.*?)\)', {
                'backward': True,
                'scope': 'keyword.other.go'
              }
            ) or
          __import__('Expression').
            expression.
            find_match(
              view,
              view.sel()[0].a,
              'func\s*\((.*?)\)', {
                'backward': False,
                'scope': 'keyword.other.go'
              }
            ) or
          __import__('Expression').
            expression.
            find_match(
              view,
              0,
              'type (\w+)', {
                'backward': False,
                'scope': 'keyword.other.go'
              }
            ) or
          __import__('Expression').
            expression.
            find_match(view, 0, '()') # get empty match if nothing found
        ).group(1)
      )
  }
]]></eval>
  <scope>source.go</scope>
  <description>func String</description>
</snippet>