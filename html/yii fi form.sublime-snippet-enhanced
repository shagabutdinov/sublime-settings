<snippet>
	<content><![CDATA[
<?= \$form->
    field(${1:$element}, '${2:property}')->
    input('${3:text}') ?>
]]></content>
	<tabTrigger>fi</tabTrigger>
	<scope>text.html</scope>
	<context>
	  [
	    {"key": "settings.frameworks", "operator": "regex_contains", "operand": "'yii'"},
	  ]
	</context>
	<description>form</description>
	<eval><![CDATA[{
		'element': (__import__('Expression').expression.find_text(view, view.sel()[0].begin(), r'field\((.*?),', {'nesting': True, 'backward': True}) or
			__import__('Expression').expression.find_text(view, view.sel()[0].begin(), r'field\((.*?),', {'nesting': True}) or
			'').replace('$', '\$')
	}]]></eval>
</snippet>