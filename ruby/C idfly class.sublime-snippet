<snippet>
    <content><![CDATA[
class IdFly::${1:$class}

	extend IdFly::Import

	def self._import(helper)
	  return {

	  }
	end

  $0

end
]]></content>
  <tabTrigger>C</tabTrigger>
  <scope>source.ruby(?!.*source)</scope> // allow only in html but not in source code
  <description>class</description>
  <eval><![CDATA[
{
	'class': __import__('User').snippets.get_class_name(view.file_name()) or ''
}
]]></eval>
</snippet>